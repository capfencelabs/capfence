# Payments Policy Pack

Use this pack for `payment.create`, `refund.issue`, `transfer.initiate`, and `invoice.send`.

Recommended metadata: amount, currency, tenant, environment, recipient verification status, recipient age, invoice ID, reason, approval role, and original payment method. Financial actions should retain audit metadata sufficient to reconstruct who approved what and why.
