# Kubernetes and Infrastructure Policy Pack

Use this pack for `k8s.apply`, `k8s.delete`, `k8s.exec`, `cloud.instance.modify`, and `terraform.apply`.

Recommended metadata: environment, namespace, resource kind, resource name, tenant, cloud account, plan summary, and whether the request mutates IAM, ingress, firewall, or secret resources.
